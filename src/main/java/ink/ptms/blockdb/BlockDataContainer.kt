package ink.ptms.blockdb

import org.bukkit.block.Block

/**
 * blockdb
 * ink.ptms.blockdb.BlockDataContainer
 *
 * @author sky
 * @since 2021/5/12 10:00 下午
 */
class BlockDataContainer(val world: String, val blockX: Int, val blockY: Int, val blockZ: Int) : DataContainer()